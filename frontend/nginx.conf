worker_processes auto;

error_log /var/log/nginx/error.log warn;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    # Protect the 1 GiB box / cold-start window (V3.3 §1 rate limiting).
    limit_req_zone $binary_remote_addr zone=hexa_frontend:10m rate=20r/s;

    sendfile        on;
    tcp_nopush      on;
    keepalive_timeout  65;

    server {
        listen       80;
        listen       [::]:80;
        server_name  _;
        server_tokens off;
        absolute_redirect off;

        root   /usr/share/nginx/html;
        index  index.html;

        # Don't accept unbounded bodies on the static frontend either
        # (V3.3 §1) — uploads go to the API, this is a flat-file server.
        client_max_body_size 1m;

        # Security headers (Phase 2) — see nginx/security-headers.conf.
        include /etc/nginx/conf.d/security-headers.conf;

        # Static assets - cache aggressively
        location /_next/static/ {
            include /etc/nginx/conf.d/security-headers.conf;
            add_header Cache-Control "public, immutable, max-age=31536000";
            try_files $uri =404;
        }

        # Static files (images, txt, etc)
        location ~* \.(png|jpg|jpeg|gif|ico|svg|txt|woff|woff2|ttf|eot|pdf|json)$ {
            include /etc/nginx/conf.d/security-headers.conf;
            add_header Cache-Control "public, max-age=86400";
            try_files $uri =404;
        }

        # SPA routing - try the file, then the directory index, then fall back to index.html
        location / {
            limit_req zone=hexa_frontend burst=40 nodelay;
            try_files $uri $uri/ $uri/index.html =404;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}
